% Installation file. Adds local folders to path.

fprintf('Adding PGEToolbox folders to Matlab path... ')

addpath(fullfile(pwd));

fprintf('done.\n')
disp('Type "savepath" if you wish to store changes.')
% savepath;
